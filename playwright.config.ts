import { createcampushelpConfig } from "campushelp-agent-playwright-config/config";

export default createcampushelpConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
